# Linear API key — get yours at https://linear.app/settings/api
VITE_LINEAR_API_KEY=lin_api_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# GitHub: Uses GitHub CLI (gh) for authentication
# Run 'gh auth login' to authenticate — no env var needed
